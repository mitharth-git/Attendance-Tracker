import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mitharth.attendancetracker',
  appName: 'Attendance Tracker',
  webDir: 'dist'
};

export default config;
